	LDAI 511
	ADDI 511
	STA R1
	STA R1
	LDAI 0
	STA R0

	LDA R0
	STA R0
	SW R0 R0
	ADDI 1
	STA R0
	SUB R1
	BLT -7

	LDAI 1
	STA R0

	LDA R0
	ADDI 1
	STA R0
	STA R2

	LDA R2
	ADD R0
	STA R2
	SW R5 R2
	SUB R1
	BLT -6

	LDA R0
	SUBI 24
	BLT -13

	LDAI 511
	ADDI 66
	STA R4
	LDAI 0
	LW R4 R4
	STA R3
	JMP 34
	

	LDAI => Load Accumulator with Immediate
	LDA  => Load Accumulator
	ADD  => Add to Accumulator
	ADDI => Add immediate to accumulator
	SUB  => Sub from accumulator
	SUBI => Sub immediate from accumulator
	STA  => Store Accumulator's value
	JMP  => Jump
	BGT  => Branch if Greater Than
	BLT  => Branch if Less Than